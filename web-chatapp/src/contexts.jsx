import {createContext} from 'react'


export let chatbuf = createContext(null); // provided inside dashboard

export const screenSt = createContext(null); // provided inside dashboard

export const ThreadData = createContext(null); // provided inside dashboard

export const backBut = createContext(null); // provided inside dashboard

export const supabaseClient = createContext(null); // provided inside index

export const userData = createContext(null); // provided inside dashboard

export const ThreadInfo = createContext(null); // provided inside dashboard

export const GetPerson = createContext(null); // provided inside dashboard

export const editYourInfo = createContext(null); // provided inside dashboard
    